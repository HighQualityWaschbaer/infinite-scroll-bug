import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'infinite-scroll-bug',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
